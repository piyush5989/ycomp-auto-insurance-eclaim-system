package com.yclaims.kernel.domain;

/**
 * Marker interface for Value Objects.
 * Value objects are immutable; equality is based on attribute values, not identity.
 */
public interface ValueObject {
}
